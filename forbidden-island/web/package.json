{
  "name": "forbidden-island-web",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Vite/React frontend for forbidden-island/ - kept as its own npm project so React/Vite deps never enter the server's production Dockerfile stage (same pattern as patchwork/web).",
  "scripts": {
    "dev": "vite",
    "build": "vite build"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.4",
    "vite": "^5.4.11"
  }
}
